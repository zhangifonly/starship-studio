# 中文解说与镜头规划

主稿位于 `src/launch-timeline.ts`，与动画阶段共用时间轴。执行
`npm run narration:plan` 可生成 `public/narration/plan.json` 和
`public/narration/subtitles.srt`，无需语音 API 密钥。

## 综合演示

166 秒、12 段中文讲稿，已生成两套共 24 段 MP3。沿用 phyviz 的 Microsoft
Edge TTS 配置：默认 `zh-CN-YunxiNeural`（云希），可切换
`zh-CN-XiaoxiaoNeural`（晓晓）；生成参数 `rate=+0%`、`volume=+0%`、`pitch=+0Hz`。

音频齐全时默认开启解说，点击播放演示后由 HTMLAudioElement 播放，
不依赖系统安装的语音；页面载入时演示和音频均暂停，不强制自动播放。
MP3 不随开源仓库分发，缺少音频时按钮禁用；生成后需重启服务器或重新构建。
暂停后继续原位置，倍速保持音高且不重读，镜头切换不打断，离开视图即停止。
跳转和切换音色定位到当前阶段的实际秒数；若已超过该句长度则保持静音，
直到下一段。缓冲时同步暂停演示时钟；失败时保留字幕并提示关闭后重试。
后台标签页暂停音频与演示，回来后重新同步。
缓冲和错误提示使用固定高度状态区，换段不改变画布尺寸；真实窗口尺寸
变化时，画布缩放与重新绘制在同一帧完成，避免短暂空白。

MP3 时长由 mutagen 解析并经浏览器解码核对，全部小于阶段预算，预留至少
0.2 秒间隔。不为塞入阶段而加快生成语速。自动验收检查真实波形、解码、
同步与控件行为，不代表已由人工听审音色或每个专有名词。

## 镜头与叙事

| 时间 | 阶段 | 默认镜头重点 |
| --- | --- | --- |
| 00:00–00:06 | 发射准备 | 发射塔与完整箭体 |
| 00:06–00:12 | 发动机点火 | 尾焰、地面烟汽 |
| 00:12–00:24 | 起飞离塔 | 整箭跟随 |
| 00:24–00:40 | 上升转弯 | 倾转与地球边缘 |
| 00:40–00:52 | 热分离 | 两级同时入画 |
| 00:52–01:08 | 助推器返航 | 翻转、返航点火 |
| 01:08–01:22 | 下降制动 | 回到发射场 |
| 01:22–01:34 | 塔架捕获 | 机械臂承接助推器 |
| 01:34–01:52 | 星舰滑行 | 切回星舰与地球背景 |
| 01:52–02:16 | 大气再入 | 姿态与热防护面辉光 |
| 02:16–02:34 | 翻转着陆 | 重启减速、概念支架展开、对准平台 |
| 02:34–02:46 | 平台着陆 | 支脚接触甲板、关机、流程结束 |

用户可随时切换导演镜头、跟随星舰、跟随助推器、地面机位、地球全景、海上平台、发射场俯视。
两级共享同一演示时间轴；导演镜头只切换观察对象，不把同时发生的运动
伪装成真实的连续任务录像。所有时间、航程和物体比例均为流程演示参数。

## 重新生成

```sh
python3 -m venv .venv-tts
.venv-tts/bin/pip install -r scripts/tts-requirements.txt
npm run narration:plan
.venv-tts/bin/python scripts/generate-narration.py
npm run test:narration
npm run build
```

生成需要访问微软语音服务；部署和用户播放仅访问本站静态 MP3，不需要
语音服务密钥或运行 Python 后端。文件名包含讲稿与音色的内容指纹，避免
旧缓存；生成器缓存成功文件、重试失败请求，仅在全部通过时写入音频清单。
浏览器清单为 `src/narration-audio.json`，公开稿件为 `public/narration/plan.json`。
SRT 仍然是阶段级字幕，不是逐字对齐。

内容约束：不宣称这是一项真实任务，不编造遥测。末两段解说明确标注海上
平台和支架为未验证的概念设计，不混淆星舰与猎鹰九号已验证的无人船回收，
也不将星舰塔架捕获表现为已经验证的能力。

## Flight 5 捕获复盘

新增历史复盘使用独立的 35 秒、5 段讲稿，源文件为 `src/mission-data.ts`。
云希和晓晓共 10 段 MP3，保持与综合演示相同的音色和生成参数，但不复用
含 V3 / 平台概念的讲稿。生成后实测每句短于阶段预算至少 0.2 秒。

```sh
npm run narration:capture
.venv-tts/bin/python scripts/generate-narration.py --plan public/narration/capture-plan.json --manifest src/capture-audio.json
npm run test:capture
npm run test:mission
```

`src/capture-audio.json` 是播放清单，`public/narration/capture-subtitles.srt`
是片段字幕。音频缺失时独立禁用任务语音，不影响原有演示。
两套体验共用音频同步逻辑，默认有声但必须先点击播放；离开页面即暂停。
自然换段不重建三维画布，不改变字幕和状态栏的高度。

这里的历史事实是 Flight 5 确实捕获了 B12；35 秒只是重建片段时钟，
不是真实任务的精确 T+ 时间，旁白也不应给未测量的运动参数背书。
